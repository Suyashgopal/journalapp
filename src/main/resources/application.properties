spring.application.name=journalApp

# MongoDB connection lives in application.yml as spring.data.mongodb.uri (env-driven).
# Defining host/port here too would conflict with that uri and trigger a Spring Boot
# warning, so it was removed.

# NOTE: CORS is configured programmatically in springsecurity.CorsConfigurationSource.
# The previous spring.web.cors.* keys are not recognized by Spring Boot and were removed.
